<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.pcitc.mapper.UserMapper">
	<select id ="getUserById" resultType="com.pcitc.bean.User">
	    SELECT 
		id,
		username,
		password
		FROM user WHERE id = ?
	</select>
</mapper>